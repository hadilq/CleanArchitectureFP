<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.github.hadilq.cleanarchitecturefp"
        >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop"
            android:name=".App"
            tools:ignore="GoogleAppIndexingWarning"
            >
        <activity android:name="com.github.hadilq.presentationartists.ArtistsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.github.hadilq.presentationalbums.AlbumsActivity"/>
        <activity android:name="com.github.hadilq.presentationalbumdetails.AlbumDetailsActivity"/>

    </application>

</manifest>